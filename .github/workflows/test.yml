name: Run Full Test Suite

on:
  push:
    branches-ignore: [main]  # Игнорируем прямые пуши в main
  pull_request:
    branches: [main]         # Проверка PR в main из любой ветки

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install Node dependencies
        run: npm install

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.1'

      - name: Install Flutter dependencies
        run: |
          cd src/flutter_ui
          flutter pub get

      - name: Run full test suite
        run: npm run test
